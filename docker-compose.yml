version: '3.8'

services:
  db:
    restart: always
    image: postgres:16.3-alpine
    volumes:
    - db:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=qwerty
    ports:
      - "5436:5432"

volumes:
  db: